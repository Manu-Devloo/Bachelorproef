services:
  ctfd:
    environment:
      CTFD_CONTAINER_DOCKER_HOST: unix:///var/run/docker.sock
      CTFD_CONTAINER_PUBLIC_HOST: 127.0.0.1
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
